# ===========================================
# Configuración para Docker (Local)
# ===========================================

# PostgreSQL Local (Docker)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=admin_edificios

# URL de conexión a la base de datos
# Para desarrollo local con Docker:
DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/admin_edificios"

# Para uso dentro de Docker Compose (la app usa este valor automáticamente):
# DATABASE_URL="postgresql://postgres:postgres123@postgres:5432/admin_edificios"

# ===========================================
# Supabase (Opcional - para autenticación)
# ===========================================
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
